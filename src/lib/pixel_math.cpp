#include "pixel_math.h"

namespace hipscat{

std::vector<std::vector<long long int> > generate_alignment(
    const std::vector<long long int> &histogram,
    short highest_order,
    short lowest_order,
    long long int threshold
){
    std::vector<std::vector<long long int> >  result (histogram.size());

    // TODO - implement.

    return result;
}

}